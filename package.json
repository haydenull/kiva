{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "node scripts/test.js",
    "dev": "yarn workspace @wozjs/kiva-ui dev",
    "build:all": "yarn workspace @wozjs/kiva-ui build:all",
    "build:component": "yarn workspace @wozjs/kiva-ui build:component",
    "build:site": "yarn workspace @wozjs/kiva-ui build:site",
    "build": "yarn workspace @wozjs/kiva-ui build",
    "deploy": "yarn workspace @wozjs/kiva-ui deploy"
  },
  "devDependencies": {
    "jest": "^26.4.2",
    "lerna": "^3.22.1",
    "minimist": "^1.2.5"
  }
}
